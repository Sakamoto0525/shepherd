version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: ./Dockerfile
      target: local
    tty: true
    stdin_open: true
    command: "air -c .air.toml"
    volumes:
      - .:/go/src/github.com/Sakamoto0525/coreos
    ports:
      - 8080:8080
    networks:
      - coreos-network

networks:
  coreos-network:
    external:
      name: coreos-network
